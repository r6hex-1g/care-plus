import SwiftUI

struct ChatView: View {
    var body: some View {
        VStack {
            Image(systemName: "bubble.fill")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("채팅 구현 예정")
        }
    }
}
